modules:
  enabled:
    - WebDriver:
        url: http://localhost:5001
        browser: chrome
        port: 9515

extensions:
  enabled:
    - \Helper\WaitRunProcess:
      - vagrant ssh cmfive --command 'php /vagrant/selenium-testrunner/CleanAndSeed.php /vagrant/site_dev/cmfive-boilerplate'
